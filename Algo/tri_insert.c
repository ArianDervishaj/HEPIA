#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>
#include <time.h>
#include <math.h>

int main() {
    int size = 10;
    int tab[] = {4,2,6,10,30,11,23,21,65,39};

    for(int i = 1; i < size; i++){
        for(int j = 0; j < i; j++){
            if(tab[i] > tab[j]){
                for()
            }
            int tmp = tab[i];

        }
    }

    for(int i = 0; i< size; i++){
        printf("%d, ", tab[i]);
    }
    return 0;
}
